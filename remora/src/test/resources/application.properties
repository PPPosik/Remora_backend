#Multipart
spring.servlet.multipart.file-size-threshold=100MB
spring.servlet.multipart.max-file-size=10GB
spring.servlet.multipart.max-request-size=10GB

papago.id=${env.PAPAGO_ID}
papago.pw=${env.PAPAGO_PW}
video-path=${env.VIDEO_PATH}
frame-path=${env.FRAME_PATH}
classification-module-path=${env.CLASSIFICATION_MODULE_PATH}
classification-result-path=${env.CLASSIFICATION_RESULT_PATH}
ocr-module-path=${env.OCR_MODULE_PATH}
ocr-result-path=${env.OCR_RESULT_PATH}